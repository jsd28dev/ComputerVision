# ---------------------------------------------------------------------------
# Runtime dependencies for smalldet.
#
# torchvision >= 0.16 is required: the data pipeline is built on transforms.v2
# and tv_tensors, which is what keeps boxes in sync with pixels through
# geometric augmentation. Anything older exposes the API as `datapoints`.
# ---------------------------------------------------------------------------
torch>=2.1
torchvision>=0.16

numpy>=1.24
pillow>=10.0
pyyaml>=6.0

# Visualization module (comparison grids, area histograms, PR curves).
matplotlib>=3.7

# The web UI. Verified against 6.24; the Blocks API this project uses (elem_id,
# api_name, queue/launch kwargs) is stable across 4.x-6.x.
# gradio_client ships with gradio; tests/e2e drives the running app through it.
gradio>=4.44

# ---------------------------------------------------------------------------
# Optional — everything below has a working fallback, install if you want it.
#
#   pycocotools   Cross-check reference for smalldet.evaluation.coco_eval. The
#                 project ships its own NumPy COCOeval implementation so AP,
#                 AP_small and AP_medium work without it; when pycocotools IS
#                 installed, tests/test_coco_eval.py asserts the two agree.
#   torchmetrics  Second independent mAP cross-check (same purpose).
#   tensorboard   Enables the TensorBoardLogger training callback.
#   psutil        Nicer device/memory reporting in the training log.
# ---------------------------------------------------------------------------
pycocotools>=2.0.7
torchmetrics>=1.0
tensorboard>=2.14
psutil>=5.9
